import React from 'react';
import { Loading } from '@components';

export default {
  title: 'Loading'
};

export const LoadingScreenStory = () => <Loading />;

LoadingScreenStory.story = {
  name: 'Loading Screen'
};
